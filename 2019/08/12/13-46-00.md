# [PATCH 1/1] Cygwin: console: Fix deadlock at calling fork().

| Header | Value |
|--------|-------|
| **From** | Takashi Yano <takashi.yano@nifty.ne.jp> |
| **To** | cygwin-patches@cygwin.com |
| **Date** | 2019-08-12T13:46:00Z |
| **Message-ID** | [20190812134623.2102-2-takashi.yano@nifty.ne.jp](https://inbox.sourceware.org/cygwin-patches/20190812134623.2102-2-takashi.yano@nifty.ne.jp) |

---

**Thread**: [thread](13-46-00.thread.md#:~:text=2019/08/12/13-46-00)

- Calling fork() on console occasionally falls into deadlock. The reason
  is not clear, however, this patch fixes this problem anyway.
---
```
 winsup/cygwin/fhandler_console.cc | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

```

```diff
diff --git a/winsup/cygwin/fhandler_console.cc b/winsup/cygwin/fhandler_console.cc
index 3d26a0b90..4afb7efb7 100644
--- a/winsup/cygwin/fhandler_console.cc
+++ b/winsup/cygwin/fhandler_console.cc
@@ -168,8 +168,12 @@ fhandler_console::set_unit ()
       if (created)
 	con.owner = getpid ();
     }
-  if (!created && shared_console_info && kill (con.owner, 0) == -1)
-    con.owner = getpid ();
+  if (!created && shared_console_info)
+    {
+      pinfo p (con.owner);
+      if (!p)
+	con.owner = getpid ();
+    }
 
   dev ().parse (devset);
   if (devset != FH_ERROR)
-- 
```

2.21.0


