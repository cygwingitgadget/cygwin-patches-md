# [PATCH] Cygwin: select: Fix a bug on closing pi->bye event.

| Header | Value |
|--------|-------|
| **From** | Takashi Yano <takashi.yano@nifty.ne.jp> |
| **To** | cygwin-patches@cygwin.com |
| **Date** | 2020-08-27T18:46:20+09:00 |
| **Message-ID** | [20200827094620.591-1-takashi.yano@nifty.ne.jp](https://inbox.sourceware.org/cygwin-patches/20200827094620.591-1-takashi.yano@nifty.ne.jp) |

---

**Thread**: [thread](09-46-20.thread.md#:~:text=2020/08/27/09-46-20)

- Close event handle pi->bye only if it was created.
```
  Addresses:
  https://cygwin.com/pipermail/cygwin-developers/2020-August/011948.html
```

---
```
 winsup/cygwin/select.cc | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

```

```diff
diff --git a/winsup/cygwin/select.cc b/winsup/cygwin/select.cc
index 9f1a8a57a..501714fa7 100644
--- a/winsup/cygwin/select.cc
+++ b/winsup/cygwin/select.cc
@@ -783,8 +783,8 @@ pipe_cleanup (select_record *, select_stuff *stuff)
       pi->stop_thread = true;
       SetEvent (pi->bye);
       pi->thread->detach ();
+      CloseHandle (pi->bye);
     }
-  CloseHandle (pi->bye);
   delete pi;
   stuff->device_specific_pipe = NULL;
 }
@@ -978,8 +978,8 @@ fifo_cleanup (select_record *, select_stuff *stuff)
       pi->stop_thread = true;
       SetEvent (pi->bye);
       pi->thread->detach ();
+      CloseHandle (pi->bye);
     }
-  CloseHandle (pi->bye);
   delete pi;
   stuff->device_specific_fifo = NULL;
 }
@@ -1344,8 +1344,8 @@ pty_slave_cleanup (select_record *me, select_stuff *stuff)
       pi->stop_thread = true;
       SetEvent (pi->bye);
       pi->thread->detach ();
+      CloseHandle (pi->bye);
     }
-  CloseHandle (pi->bye);
   delete pi;
   stuff->device_specific_ptys = NULL;
 }
-- 
```

2.28.0



